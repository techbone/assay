# Fly.io app config for Assay: collector + read API in one always-on machine,
# backed by a persistent volume so raw observations survive restarts/deploys.
app = "assay-hackathon"
primary_region = "iad"

[build]

[env]
  PORT = "8787"

[[mounts]]
  source = "assay_data"
  destination = "/app/data"

[[services]]
  internal_port = 8787
  protocol = "tcp"
  # Must be the string "off", not `false`. As a boolean this was ignored and flyd
  # stopped the machine after 5 minutes idle (exit_code=130, requested_stop=true),
  # so the collector only ran while someone was loading the site.
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1

  [[services.ports]]
    port = 80
    handlers = ["http"]
    force_https = true

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]

  [[services.tcp_checks]]
    interval = "15s"
    timeout = "5s"

  [[services.http_checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "30s"
    method = "get"
    path = "/api/health"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
